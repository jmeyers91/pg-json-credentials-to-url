{
  "name": "pg-json-credentials-to-url",
  "version": "1.0.2",
  "description": "A utility for converting from raw JSON Postgres credentials to a Postgres connection URL.",
  "main": "./dist/index.js",
  "bin": "./dist/cli.js",
  "scripts": {
    "test": "npx jest",
    "build": "npx tsc && chmod 755 ./dist/cli.js",
    "prepublishOnly": "npm run test && npm run build",
    "publish": "npm publish"
  },
  "keywords": [
    "postgres",
    "pg",
    "url",
    "rds",
    "json"
  ],
  "author": "James Meyers",
  "devDependencies": {
    "@types/jest": "^29.4.0",
    "jest": "^29.5.0",
    "ts-jest": "^29.0.5",
    "typescript": "^4.9.5"
  }
}
